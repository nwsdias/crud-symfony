<form method="post" action="{{ path('cliente_delete', {'cpf': cliente.cpf}) }}" onsubmit="return confirm('Tem certeza que deseja excluir esse item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cliente.cpf) }}">
    <button class="btn btn-danger">Deletar</button>
</form>
